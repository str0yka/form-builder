import { Typography } from '@mui/material';

type SeparatorFieldControllerProps = { id: number } & FormElementExtraAttributes['SeparatorField'];

export const SeparatorFieldController: React.FC<SeparatorFieldControllerProps> = () => (
  <Typography>No properties for this element</Typography>
);
